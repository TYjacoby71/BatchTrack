"""Data Builder Service (separate from main Flask app).

This is a lightweight web UI/process for running `data_builder/` scripts on-demand.
It intentionally does *not* run any builder tasks automatically on startup.
"""


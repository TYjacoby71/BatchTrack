✅ Fix Request Summary (For Replit Assistant)
🔧 Please fix the following in the recipe creation form:
🟠 1. Add a Description Field
Problem: Description field is missing.

Solution: In the recipe form template (e.g. recipe_edit.html), add:

html
Copy
Edit
<label for="description">Description:</label>
<textarea name="description" rows="3" placeholder="e.g. Soothing balm for dry skin">{{ recipe.description if recipe else '' }}</textarea>
Also, ensure the POST route for saving a recipe includes:

python
Copy
Edit
recipe["description"] = request.form.get("description", "")
🟡 2. Show Structured Category Dropdowns
Problem: No dropdowns for product type, use area, etc.

Solution: In the recipe form template, add these dropdowns using category_options:

html
Copy
Edit
<label>Product Type:</label>
<select name="product_type">
  {% for pt in category_options.product_types %}
    <option value="{{ pt }}" {% if recipe.product_type == pt %}selected{% endif %}>{{ pt }}</option>
  {% endfor %}
</select>

<label>Use Area:</label>
<select name="use_area">
  {% for ua in category_options.use_areas %}
    <option value="{{ ua }}" {% if recipe.use_area == ua %}selected{% endif %}>{{ ua }}</option>
  {% endfor %}
</select>

<label>Use Case:</label>
<select name="use_case">
  {% for uc in category_options.use_cases %}
    <option value="{{ uc }}" {% if recipe.use_case == uc %}selected{% endif %}>{{ uc }}</option>
  {% endfor %}
</select>

<label>Primary Ingredient:</label>
<select name="primary_ingredient">
  {% for pi in category_options.primary_ingredients %}
    <option value="{{ pi }}" {% if recipe.primary_ingredient == pi %}selected{% endif %}>{{ pi }}</option>
  {% endfor %}
</select>
Also make sure these values are saved in the recipe POST route:

python
Copy
Edit
recipe["product_type"] = request.form.get("product_type")
recipe["use_area"] = request.form.get("use_area")
recipe["use_case"] = request.form.get("use_case")
recipe["primary_ingredient"] = request.form.get("primary_ingredient")
🔴 3. Add New Ingredients Inline
Problem: If an ingredient doesn’t exist in inventory (e.g. Tallow), it can’t be selected or added.

Fix Suggestion (Short-Term): Add a “+ Add Ingredient” button next to the ingredient dropdowns that links to /ingredients/add.

Add this next to the dropdown in the recipe form:
html
Copy
Edit
<a href="/ingredients/add" target="_blank" style="font-size: 12px; margin-left: 10px;">➕ Add new ingredient</a>
This opens a new tab and lets the user add ingredients while staying on the recipe page.

✅ Summary to Send to Assistant:
sql
Copy
Edit
Please fix the recipe creation form to include:

1. A textarea for description input (save to recipe["description"])
2. Four structured dropdowns: product_type, use_area, use_case, primary_ingredient — using category_options
3. Add a "+ Add New Ingredient" link next to ingredient selector, pointing to /ingredients/add

Also make sure all of these values are captured in the POST route that saves the recipe.

Thank you!

✅ This Batch Adds:
Feature	Description
/ route	Landing dashboard
Links to key pages	Ingredients, Recipes, Batches, New Batch
Optional	Show low-stock ingredients (under threshold)
Optional	Show latest 5 batches
✅ Replit Instructions — Copy & Paste This
🧾 Step 1: Add Home Route
css
Copy
Edit
Please add this to `main.py` or `batches.py`:

```python
@app.route('/')
def dashboard():
    data = load_data()
    low_stock = [i for i in data['ingredients'] if float(i['quantity']) < 10]  # Adjust threshold
    recent_batches = sorted(data.get('batches', []), key=lambda b: b['timestamp'], reverse=True)[:5]
    return render_template("dashboard.html", low_stock=low_stock, recent_batches=recent_batches)
yaml
Copy
Edit

---

### 🧾 Step 2: Create `dashboard.html`

Please create a new template file: templates/dashboard.html

html
Copy
Edit
{% extends "layout.html" %}
{% block content %}

<h1>📋 BatchTrack Dashboard</h1>

<p>Welcome, {{ session.get("user", "Guest") }}!</p>

<h3>Quick Links</h3>
<ul>
  <li><a href="/ingredients">Manage Ingredients</a></li>
  <li><a href="/recipes">View Recipes</a></li>
  <li><a href="/batches">Production History</a></li>
  <li><a href="/check-stock-bulk">Bulk Stock Check</a></li>
</ul>

<h3>⚠️ Low Stock Ingredients</h3>
{% if low_stock %}
<table border="1" cellpadding="5">
  <tr><th>Name</th><th>Qty</th><th>Unit</th></tr>
  {% for ing in low_stock %}
    <tr>
      <td>{{ ing.name }}</td>
      <td>{{ ing.quantity }}</td>
      <td>{{ ing.unit }}</td>
    </tr>
  {% endfor %}
</table>
{% else %}
<p>All stock levels look good ✅</p>
{% endif %}

<h3>🕒 Recent Batches</h3>
{% if recent_batches %}
<table border="1" cellpadding="5">
  <tr><th>Recipe</th><th>Time</th><th>Cost</th></tr>
  {% for b in recent_batches %}
    <tr>
      <td>{{ b.recipe_name }}</td>
      <td>{{ b.timestamp[:16].replace('T', ' ') }}</td>
      <td>${{ b.total_cost }}</td>
    </tr>
  {% endfor %}
</table>
{% else %}
<p>No batches yet.</p>
{% endif %}

{% endblock %}
yaml
Copy
Edit

---

### ✅ What This Gives You:

- 🚀 Landing page with real info
- ✅ Highlights stock alerts
- 🧠 Recent batches view
- 🔗 Navigation links

---

Would you like me to:
- Add a **navigation bar** in `layout.html` next?
- Color-code low stock rows (e.g. red)?
- Add filtering by tag/date directly on dashboard?
